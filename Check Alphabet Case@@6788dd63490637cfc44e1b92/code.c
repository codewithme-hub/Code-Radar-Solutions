#include <stdio.h>
int main() {
    int character;
    scanf("%d",&character);
    if(character='z'){
        printf("Lowercase");
    }
    else if(character='A'){
        printf("Uppercase");
    }

    //printf("%s", welcome());
    return 0;
}